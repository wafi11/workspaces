apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: terminal-pvc
  namespace: {{.Namespace}}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi